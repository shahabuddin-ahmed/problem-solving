const TreeNode = require("./TreeNode");

class Solution {
	/**
	 * Returns the data of the kth largest node
	 * in the passed binary search tree.
	 *
	 * @param {TreeNode} root Root TreeNode of the binary search tree.
	 * @param {number} k Target (kth largest).
	 * @return {number} Data of the Kth largest node.
	 */
	kthLargest(root, k) {
		
	}

}

module.exports = Solution;